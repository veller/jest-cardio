// challenges:
// - lib mocks
// - spyOn, jest,fn(), jest.mock(), manual mock, mockImplementation()

describe("Router", () => {
  it("should call Router() on express and attribute to router variable", () => {});
  it("should call .get() on router", () => {});
});
