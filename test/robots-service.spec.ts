// challenges:
// - lib mocks
// - mock function
// - error
// - spyOn, jest,fn(), jest.mock(), manual mock, mockImplementation()

describe("Index", () => {
  it("should return the robots information", () => {});
  it("should return an error when something goes wrong retrieving robots information", () => {});
});
