// challenges:
// - lib mocks
// - spyOn, jest,fn(), jest.mock(), manual mock, mockImplementation()

describe("Index", () => {
  it("should log the method and path each time a request is made", () => {});
  it("should call express next()", () => {});
});
